pre-commit:
  parallel: true
  commands:
    install:
      run: rustup component add rustfmt clippy
    build:
      run: cargo build
    test:
      run: cargo test
    fmt:
      run: cargo fmt --all -- --check
    clippy:
      run: cargo clippy -- -D warnings
